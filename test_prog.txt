# Test program for snapshot functionality

# Set initial register values
li $1, 111
li $2, 222

# Take a snapshot before registers are changed
# The PC will be 2 when this is executed.
SNAPSHOT test_snapshot.snap

# Modify registers after the snapshot
li $1, 999
add $3, $1, $2

# Dump the final state for verification
DUMP_PROCESSOR_STATE
